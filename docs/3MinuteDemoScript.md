# Option C: 3-Minute Demo Script (Updated for AI Workflows)

**Total Time**: â‰¤ 3 minutes

### 0:00 â€“ 0:40 â€” Introduction (40s)
"Today we're building a Smart Community App Generator using the AI workflows and development approach from the smart-assets.io GitLab profile. It helps entrepreneurs create regenerative community apps. The process starts with an AI-orchestrated Voice & Story step, followed by agent-driven generation and built-in contribution-reward loops."

### 0:40 â€“ 1:40 â€” Voice & Story Creation â€” Start of AI Workflow (60s)
Live demo:
- Enter a prompt about a healthy living community app.
- Show the VoiceStoryAgent (first node in the LangGraph AI workflow) in action.
- Display the generated Voice Profile + Narrative.
- "This initiates the full AI-orchestrated workflow. The story will guide all subsequent agents and future community contributions/reviews."

### 1:40 â€“ 2:30 â€” AI Workflow Generation & Live Preview (50s)
- Trigger the full multi-agent LangGraph workflow (following smart-assets.io AI development approach).
- Show the Clean Health Smart App preview with contribution â†’ reward in action.
- Demonstrate a review/contribution: Submit feedback â†’ it is processed as a rewarded contribution in the workflow.

### 2:30 â€“ 3:00 â€” Close & Vision (30s)
- Highlight how the entire process follows AI workflows with built-in contribution-reward loops (the better business model in action).
- Mention future smart contract / ZK integration (show placeholder in UI).
- "This generator itself demonstrates the smart-assets.io AI development approach and produces apps that reward community contributions."
- End with deployed link, architecture overview (Mermaid of the LangGraph workflow), and how it extends BountyForge/Smart Assets.

**Tips**:
- Practice timing.
- Use screen sharing of live deployed app.
- Have a Mermaid diagram of the full AI workflow (VoiceStoryAgent â†’ downstream agents â†’ review loop) ready as backup slide.
- Emphasize that the generator uses the same AI development approach as outlined in the smart-assets.io GitLab profile.
